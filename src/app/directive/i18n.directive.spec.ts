import { I18nDirective } from './i18n.directive';

describe('I18nDirective', () => {
  it('should create an instance', () => {
    const directive = new I18nDirective();
    expect(directive).toBeTruthy();
  });
});
